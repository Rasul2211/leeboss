# Tuned for a slow link: the default 5-minute idle timeout aborts mid-install here.
fetch-timeout=1800000
fetch-retries=8
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=600000
# fewer parallel sockets keeps each one alive instead of starving them all
maxsockets=3
audit=false
fund=false
